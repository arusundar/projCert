
---
- hosts: 13.222.111.250 ansible_user=root ansible_password=Accenture1!
  become: yes
  tasks:
    - name: Install Docker
      yum:
        name: docker
        state: present

    - name: Enable and start Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Add root to docker group
      user:
        name: root
        groups: docker
        append: yes

